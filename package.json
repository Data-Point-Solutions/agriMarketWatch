{
  "name": "agrimarketwatch-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    
  },
  "devDependencies": {
    "@commitlint/cli": "^19.4.0",
    "@commitlint/config-conventional": "^19.2.2",
    "husky": "^9.1.5",
    "lint-staged": "^14.0.0"
  },
  "lint-staged": {
    "*.js": "comment-lint --fix",
    "*.jsx": "comment-lint --fix",
    "*.ts": "comment-lint --fix",
    "*.tsx": "comment-lint --fix"
  }
}
